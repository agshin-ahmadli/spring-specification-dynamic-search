spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sp_data
    username: postgres
    password: '0000'
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true
